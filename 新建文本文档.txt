http://www.patorjk.com/software/taag 生成字符的网站 例如spark标志

$(function(){
    	var secretKey = $("#secretKey").text();
    	var reg = /(^[a-z0-9_-]{2})([a-z0-9_-]{0,})([a-z0-9_-]{1})$/;
    	secretKey = secretKey.replace(reg, "$1****$3");
    	$("#secretKey").text(secretKey);
    })

web.xml is missing and <failOnMissingWebXml> is set to true
怎么新建maven 项目

阿斯蒂芬看撒即可