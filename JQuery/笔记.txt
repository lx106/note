prototype 创建每一个函数都有一个prototype属性，这个属性是一个指针，而这个指针总指向一个
对象，这个对象的用途就是将特定的属性和方法包含在内，起到所有实例共享的作用
---------------------------------------


---------------------------------
<link rel="stylesheet" href="${resources}/css/index.css?v=20171214">
如果资源更改记得为资源加上版本号,使得CDN服务器能够去重新获取
----------------------------------

$("#test1").parent(); // ?????
$("#test1").parents(); // ????????
$("#test1").parents(".mui-content");
$("#test").children(); // ???????
$("#test").children("#test1");
$("#test").contents(); // ????#test?????????????????????????
$("#test").contents("#test1");
$("#test1").prev();  // ??????????
$("#test1").prevAll(); // ???????????
$("#test1").next(); // ??????????
$("#test1").nextAll(); // ????????????
$("#test1").siblings(); // ?????????
$("#test1").siblings("#test2");
$("#test").find("#test1");

// ???��???????????????jQuery????????????????????
$("ul li").eq(1); // ??ul li??????????????1?????(??????2??li???)
$("ul li").first(); // ??ul li?????????????
$("ul li").last(); // ??ul li???????????????
$("ul li").slice(1, 4); // ????2 ~ 4?????
$("ul li").filter(":even"); // ??ul li?????????????????
----------------------------
user对象中包含address对象
<input type="hidden" value="${user.address.id}" name="address.id"/>